using HotChocolate;
using HotChocolate.Types;
using ContactApi.Models;

namespace ContactApi.GraphQL.Types
{
    //public class ContactType : ObjectType<Contact>
    //{
    //    protected override void Configure(IObjectTypeDescriptor<Contact> descriptor)
    //    {
    //        descriptor.Description("Represents a contact in the address book.");

    //        descriptor.Field(c => c.Id).Description("Represents the unique ID for the contact.");
    //        descriptor.Field(c => c.Name).Description("Represents the name of the contact.");
    //        descriptor.Field(c => c.Email).Description("Represents the email of the contact.");
    //        descriptor.Field(c => c.Phone).Description("Represents the phone number of the contact.");
    //    }
    //}
}
